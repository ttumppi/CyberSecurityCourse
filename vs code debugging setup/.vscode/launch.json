{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "node",
        "request": "attach",
        "name": "Attach to Deno App in Docker",
        "address":"localhost",
        "port": 8001,
        "localRoot": "${workspaceFolder}\\deno-app",
        "remoteRoot": "/app",
        "skipFiles": ["<node_internals>/**"],
        "restart": true,
      }
    ]
  }